.venv/
__pycache__/
*.pyc
.DS_Store

# Trained artefacts and generated data. All of it is reproducible from the
# scripts in data/ and train.py, and none of it belongs in git — the packed
# token stream alone is ~475 MB.
checkpoints/
data/corpus.jsonl
data/corpus.val.jsonl
data/*.jsonl
data/packed/

# Measured results are regenerated by eval.py / bench.py. The website bundle
# committed under website/ is generated from them.
results/
logs/
